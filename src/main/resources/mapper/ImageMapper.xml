<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.nts.pjt5_6.dao.ImageMapper">
    
    <select id="selectProductImagesInfoByIdAndType" parameterType="map" resultType="com.nts.pjt5_6.dto.ProductImages">
        SELECT product.id AS product_id,prodImage.id AS product_image_id,
        	type,
        	file_id,file_name,save_file_name,
        	content_type,
        	delete_flag,
        	flInfo.create_date,
        	flInfo.modify_date
        FROM product,
        	product_image AS prodImage,
        	file_info AS flInfo
        WHERE product.id = prodImage.product_id 
        AND  prodImage.file_id = flInfo.id 
        AND product.id = #{productId}
        <if test = "type != 'all'">
        AND type = #{type} 
        </if>
    </select>
    
    <select id="selectProductImagesInfoByProductImageId" parameterType="int" resultType="com.nts.pjt5_6.dto.ProductImages">
        SELECT product.id AS product_id,prodImage.id AS product_image_id,
        	type,
        	file_id,file_name,save_file_name,
        	content_type,
        	delete_flag,
        	flInfo.create_date,
        	flInfo.modify_date
        FROM product,
        	product_image AS prodImage,
        	file_info AS flInfo
        WHERE product.id = prodImage.product_id 
        AND  prodImage.file_id = flInfo.id 
        AND prodImage.id = #{prodImageId}
    </select>
</mapper>